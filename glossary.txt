Ritual: xampp -> apache, mySQL -> http://localhost/phpmyadmin; beca-list; npm run dev (em backend); http://localhost:3000/ping; 

cors: app.use(cors()) — ativa o CORS para todas as rotas. Isso permite que seu backend seja chamado por um frontend rodando em outra origem (outro domínio/porta), senão o navegador bloquearia essas requisições por segurança. Middleware. Estreiando;

express: o framework que cria e gerencia o servidor web. Já usei no meu último projeto. facilita a criação de rotas sem ter que lidar com detalhes complexos de rede;

npm: já usei também. Vem junto com o Node. É o gerenciador de pacotes/bibliotecas prontas — em vez de escrever do zero um jeito de conectar no banco, alguém já escreveu essa lógica e publicou como um pacote que a gente só instala e usa. npm init -y criou o package.json (a "ficha de identidade" do projeto: nome, dependências, scripts). npm install X baixa o pacote X para dentro de node_modules/;

dotenv: lê o arquivo .env e transforma cada linha dele em variável disponível no código (process.env.DB_NAME, por exemplo). Serve para manter informação sensível (senha do banco) fora do código-fonte que vai pro GitHub. Usei também, já conheço;

mysql2: o "tradutor" que sabe efetivamente conversar com um banco MySQL (protocolo de rede, autenticação, etc). Sozinho é meio complicado de usar direto. Nunca usei.

sequelize: primeira vez usando, ele é um ORM (Object-Relational Mapper). Ele usa o mysql2 por baixo, mas te deixa trabalhar com o banco escrevendo JavaScript em vez de SQL puro. Você consegue criar as tabelas automaticamente a partir dos seus models;

nodemon (só desenvolvimento): fica de olho nos arquivos do projeto e reinicia o servidor sozinho toda vez que você salva algo. Sem ele, você teria que parar (Ctrl+C) e rodar node server.js de novo a cada alteração. Já usei.

app.js: configura o Express: middlewares (cors, json) e rotas;

.env: credenciais do banco (nunca vai pro Git);

.gitignore: diz ao Git pra ignorar node_modules/ e .env;

package.json: ficha do projeto + lista de dependências + scripts;

server.js: "liga" o servidor de fato;

api.js: só sabe conversar com o backend. Não sabe nada sobre <div>, id, tela. Só recebe pedidos ("me dá as tarefas", "cria essa tarefa") e devolve dados;

ui.js: só sabe mexer no HTML/DOM. Não sabe nada sobre fetch, URLs, backend. Recebe dados prontos e só decide "como colocar isso na tela";

main.js: é o "maestro", importa o api.js e o ui.js, e conecta os dois — chama a função do api, pega o resultado, manda para a função do ui mostrar.

TAILWIND:
bg - background
text - texto
border - borda

class="p-4"    /* padding em todos os lados */ 16px
class="px-4"   /* padding só horizontal (esquerda+direita) */
class="pb-24"  /* padding só na base */ 96px
class="mb-4"   /* margin só na base */
class="gap-4"  /* espaço entre itens de um flex/grid */

class="flex items-center justify-between gap-4":
flex → liga o modo flexbox (filhos ficam lado a lado, na horizontal, por padrão).
items-center → alinha verticalmente pelo centro.
justify-between → primeiro filho gruda numa ponta, último na outra, espaço distribuído entre eles.
gap-4 → espaço fixo entre os itens.

responsividade
class="md:hidden"      /* esconde a partir do desktop */
class="hidden md:flex" /* começa escondido, aparece como flex no desktop */
class="md:max-w-md"    /* só limita largura no desktop */

visibilidade
class="hidden"        /* display: none — nem ocupa espaço */
class="hidden md:inline" /* invisível no mobile, aparece como inline no desktop */

posicionamento
class="fixed inset-0 z-40"
fixed → tira o elemento do fluxo normal da página, "prende" ele na tela (não se move com o scroll).
inset-0 → um atalho que equivale a top-0 right-0 bottom-0 left-0 juntos — cola nas 4 bordas.
z-40 → controla a "camada" de empilhamento (quem fica visualmente por cima de quem, quando elementos se sobrepõem).

bordas
lass="border-b border-divider"      /* borda só embaixo, cor divider */
class="rounded-t-2xl"                /* arredonda só os cantos de cima */
class="rounded-full"                 /* arredonda tudo (formato pílula/círculo) */
class="divide-x divide-divider"      /* linha vertical automática entre filhos */

fonte
class="text-xs"       /* tamanho pequeno */
class="text-3xl"      /* tamanho grande */
class="font-bold"     /* peso da fonte */